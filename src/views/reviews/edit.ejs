<!DOCTYPE html>
<html>

<head>
    <% include ../static/partials/head.ejs %>
</head>

<body>
    <% include ../static/partials/navbar.ejs %>

    <main class="container">
        <h1>Edit Review</h1>
        <form action="/games/<%= review.gameId %>/reviews/<%= review.id %>/update" method="post">
            <div class="form-group">
                <label for="rating">Review rating</label>
                <input value="<%= review.rating %>" type="number" class="form-control" name="rating" min="1" max="5">
            </div>
            <div class="form-group">
                <label for="body">Review body</label>
                <textarea rows="4" class="form-control" name="body" placeholder="Enter Review body"><%= review.body %></textarea>
            </div>
            <button type="submit" class="btn btn-primary">Update</button>
        </form>
    </main>

    <% include ../static/partials/baseScripts.ejs %>

</body>

</html>